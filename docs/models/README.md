# Model note

RelayFault contains no machine-learning model and makes no probabilistic prediction. Its optional probability selector is a deterministic seeded pseudo-random rule, not a learned model or risk estimate.

Observed counters describe only the current proxy process. They should not be interpreted as availability measurements for a real service.
