# Working notes, 2026-08-29

- The manual catch-up run used seed `20260829`.
- Python `random.Random(20260829)` produced mode roll `0.6032413721990276`, selecting the 70% new-project branch.
- Categories 2, 3, and 5 were excluded from the recent scheduled portfolio. The uniform eligible-category draw selected category 9, networking, distributed systems, cloud, and peer-to-peer systems.
- Portfolio review found no modern repository centered on HTTP fault injection, reproducible failure sequencing, network partitions, or distributed consistency testing.
- A real local proxy was selected over a diagram-only simulator so tests can exercise application retry behavior without modifying the application.
- The first test run passed 15 of 16 tests. The failure came from assuming Node would preserve a particular `Connection` header value. Connection management belongs to each transport hop, so the brittle integration assertion was replaced with a direct test that hop-by-hop fields are stripped while end-to-end headers remain.
- The final local run passed 18/18 tests and the demo. No application defect was concealed by the test correction.
- Node.js 26.5.0 was available locally. The project remains dependency-free and declares Node.js 22 or newer for public matrix testing.
- The README was checked for the saved punctuation preference and contains no em dash character.
